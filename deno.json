{
  "tasks": {
    "validate": "deno run --allow-read scripts/validate-commit.ts",
    "parse": "deno run --allow-run scripts/parse-commits.ts",
    "test": "deno test --allow-read --allow-run",
    "hook:install": "deno run --allow-read --allow-write --allow-run scripts/install-hooks.ts"
  }
}
